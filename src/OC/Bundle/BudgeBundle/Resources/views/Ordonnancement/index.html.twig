{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Ordonnancement list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Year</th>
                <th>Pay</th>
                <th>Date</th>
                <th>Cout</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('ordonnancement_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.year }}</td>
                <td>{{ entity.pay }}</td>
                <td>{% if entity.date %}{{ entity.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.cout }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('ordonnancement_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('ordonnancement_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('ordonnancement_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
