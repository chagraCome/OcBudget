{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Groupement list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Labelgroupement</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('groupement_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.labelGroupement }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('groupement_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('groupement_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('groupement_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
