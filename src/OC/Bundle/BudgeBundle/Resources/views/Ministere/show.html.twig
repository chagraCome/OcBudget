{% extends '::base.html.twig' %}

{% block body -%}

<div id="wrapper">

    <header class="navbar" role="banner">

    <div class="container">
     <nav class="collapse navbar-collapse" role="navigation">
			<div class="navbar-header navbar-title">
					البرمجة السنويّة للنفقات
			</div>
			
			<div class="navbar-header navbar-title">
				PAD&nbsp;&nbsp;&nbsp;<sup>V<sup>1.0<sup></sup>
			</div>
			{% include "::toolbar.html.twig" %}
		</nav>
	</div> <!-- /.container -->
  </header>
{% include "::menu_header.html.twig" %}
    <h1>Ministere</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Appel_offre</th>
                <td>{{ entity.appeloffre }}</td>
            </tr>
            <tr>
                <th>Nomministere</th>
                <td>{{ entity.nomMinistere }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('ministere') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('ministere_edit', { 'id': entity.appel_offre }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
