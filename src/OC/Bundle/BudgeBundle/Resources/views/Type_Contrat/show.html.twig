{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Type_Contrat</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Desctypecontrat</th>
                <td>{{ entity.descTypeContrat }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('type_contrat') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('type_contrat_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
