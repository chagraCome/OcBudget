{% extends '::base.html.twig' %}

{% block body -%}
<div id="wrapper">

    <header class="navbar" role="banner">

    <div class="container">
     <nav class="collapse navbar-collapse" role="navigation">
			<div class="navbar-header navbar-title">
					البرمجة السنويّة للنفقات
			</div>
			
			<div class="navbar-header navbar-title">
				PAD&nbsp;&nbsp;&nbsp;<sup>V<sup>1.0<sup></sup>
			</div>
			{% include "::toolbar.html.twig" %}
		</nav>
	</div> <!-- /.container -->
  </header>
{% include "::menu_header.html.twig" %}
    <h1>Consommation list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Year</th>
                <th>Ordonnanceengagement</th>
                <th>Ordonnancepayement</th>
                <th>Datelecture</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('consommation_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.year }}</td>
                <td>{{ entity.ordonnanceEngagement }}</td>
                <td>{{ entity.ordonnancePayement }}</td>
                <td>{% if entity.dateLecture %}{{ entity.dateLecture|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('consommation_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('consommation_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('consommation_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
